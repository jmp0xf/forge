schema = 1

[commands.check]
program = "forge-fixture-tool-does-not-exist"
args = ["check"]
cwd = "."
inputs = ["**"]
mutability = "read-only"
network = "offline-requested"
success = "exit-zero"
coverage = ["compile"]
enforcement = "required"
